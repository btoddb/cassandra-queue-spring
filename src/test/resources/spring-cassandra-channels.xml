<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
         http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration-2.0.xsd
         http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd
         ">

    <int:channel id="testPushChannel" />
    <int:channel id="testPopChannel" />

	<int:inbound-channel-adapter channel="testPopChannel" ref="cassQueueChannelAdapter" method="pop">
		<int:poller>
			<int:interval-trigger interval="100" />
            <int:transactional transaction-manager="cassQueueTxMgr" />
		</int:poller>
	</int:inbound-channel-adapter>

	<int:outbound-channel-adapter channel="testPushChannel" ref="cassQueueChannelAdapter" method="push" />

    <!-- do not need to do this in real world - only doing this for illustration and because tests need it -->
    <bean id="pusher" class="com.real.cassandra.queue.PusherImpl" factory-bean="testQueue" factory-method="createPusher" />
    
    <bean id="cassQueueChannelAdapter" class="com.real.cassandra.queue.spring.CassandraQueueChannelAdapter">
        <constructor-arg name="cassQueue" ref="testQueue" />
        <constructor-arg name="pushOnly" value="false" />
        <property name="txMgr" ref="cassQueueTxMgr" />
        <property name="pusher" ref="pusher" />
    </bean>

	<bean id="cassQueueTxMgr" class="com.real.cassandra.queue.spring.CassandraQueueTxMgr">
		<constructor-arg name="cassQueue" ref="testQueue" />
	</bean>

    <bean id="msgReceivedConsumer" class="com.real.cassandra.queue.spring.MsgReceivedConsumer" />
    <int:service-activator input-channel="testPopChannel" ref="msgReceivedConsumer" method="execute" />

</beans>

